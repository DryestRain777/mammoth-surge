/**
 * Storefront configuration.
 *
 * Paste your deployed backend URL below to enable real Stripe Checkout.
 * Leave it empty to keep the site in safe "demo" mode (buttons show a toast
 * instead of charging anyone).
 *
 * Example:
 *   apiBase: "https://mammoth-surge-api.onrender.com"
 */
window.MAMMOTH_CONFIG = {
    apiBase: ""
};
